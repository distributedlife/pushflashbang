= link_to t('actions.review-partial-answer'), '#', :class => 'btn btn-large', :method => :post, :id => "do_partial_answer"
:javascript
  $j('#do_partial_answer').click(function(event) {
    var end = new Date();
    var start_time = document.getElementById("start_time") ;
    var duration = document.getElementById("duration") ;
    elapsed = end - start_time.value;

    var link = "#{review_deck_card_path(:deck_id => @card.deck_id, :id => @card.id, :answer => 'partial_correct')}";
    link = link + "&duration=" + duration.value + "&elapsed=" + elapsed ;

    $j('#do_partial_answer').attr('href', link);
  });
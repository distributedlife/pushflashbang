=render :partial => '/header', :locals => {:title => t('card.create.title'), :subtitle => t('card.create.subtitle')}
%section{:class => ["panel"]}
  #create_card{:class => ['left'], :style => "width:100%"}
    =render :partial => 'create_card_form', :locals => {:card => @card}
  .pad
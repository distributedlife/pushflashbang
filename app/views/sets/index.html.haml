.container
  = page_header t('titles.set-index.title'), t('titles.set-index.subtitle')
  - if current_user.in_edit_mode?
    .row_fluid.span7.well
      = display_sets @sets
    .row_fluid.span3.well
      = sets_menu
  - else
    .content.well
      = display_sets @sets